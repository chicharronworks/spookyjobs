using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MejoraVeneno : MonoBehaviour
{
    public bool mejoraveneno;
    // Start is called before the first frame update
    void Start()
    {
        mejoraveneno = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
