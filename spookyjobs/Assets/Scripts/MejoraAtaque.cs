using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MejoraAtaque : MonoBehaviour
{
    public bool mejoraactiva;
    // Start is called before the first frame update
    void Start()
    {
        mejoraactiva = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
